var Mapper = require('./Mapper')

mapping = {}

module.exports = class Mapping {
        constructor(midiNote, cube){
                    this.midiNote = midiNote
                    this.cube = cube
        }
}